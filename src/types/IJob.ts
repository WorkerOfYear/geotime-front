export interface IJob {
  camera1_is_active: boolean;
  camera1_id: string | null;
  camera2_is_active: boolean;
  camera2_id: string | null;
  camera3_is_active: boolean;
  camera3_id: string | null;
}
